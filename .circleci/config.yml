version: 2.1

workflows:
  test-deploys:
    jobs:
      - test
jobs:
  test:
    working_directory: ~/Software-Engineering-Sprint1/.circleci
    docker:
    - image: cimg/node:lts
    executor: my-custom-executor
    steps:
      - checkout
          path: ~/Software-Engineering-Sprint1
      - run: npm install
